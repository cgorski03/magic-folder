#pragma once

// Aggregator header for commonly-used core types.
// Instead of including each individual header (e.g. magic_core/types/file.hpp),
// users can simply do `#include "magic_core/types.hpp"`.
//
// NOTE: Keep this list in sync with any new type headers added under
// `include/magic_core/types/`.

#include "magic_core/types/file.hpp"
#include "magic_core/types/chunk.hpp" 